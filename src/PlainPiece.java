// Represents a plain, where heroes and monsters are not affected
public class PlainPiece extends LoVPiece {
    private static final String NAME = "Plain";
    public static final String ID = "P";

    public PlainPiece() {
        super(NAME, ID);
    }
}
